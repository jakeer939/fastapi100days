Day -1:
 build a todo api using in-memory list
 what i have done:
    i have built an api, that uses lists to store data and we can add,update,delete,read from this books array/list


Day -2:
organised the todo api
learn't about fastapi routes

Day-3:
learnt about response model
polished routes in books_api(i.e to REST Style)
added status codes
response_model: it is a in-built model in fastapi that restricts sensitive-data to pass to front-end
status code:
    get -> HTTP_200_OK
    post-> HTTP_200_OK
    put -> HTTP_200_OK
    delete-> HTTP_204_NO_CONTENT

built a REST api for contact manager(get,post,put,delete)